# To Start the App

-> after forking the repo, first download all necessary npm packages by running "npm install" in the command line tool
-> install necessary packages in client as well
  (cd client -> npm install)
-> create postgresql database "Parallel" (run createdb Parallel in the terminal if postgres is already installed)
-> "npm run dev" command will start the client and the server
